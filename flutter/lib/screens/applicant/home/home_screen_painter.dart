part of '../home_screen.dart';

// Iter 139 — _DotPatternPainter removed. The home hero now uses
// AppAuroraBackground (animated multi-blob backdrop) which provides
// richer visual interest than a static dot pattern.
//
// File kept (instead of deleted) because it's referenced as a `part of`
// directive in home_screen.dart; deleting it would break the build.
