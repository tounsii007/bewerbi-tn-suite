# scripts/

Hilfs-Skripte, die mehrere Subprojekte gleichzeitig betreffen.

- `setup-all.sh` — installiert Dependencies in allen Clients
- `lint-all.sh`  — führt Linter über Web, Mobile, Flutter
- `sync-tokens.sh` — generiert plattform-spezifische Files aus `shared/tokens/`
